
{#{% extends 'base.html.twig' %}

   {% block title %}Accueil{% endblock %}


  {# {% block subtitle %}Bienvenue {{ app.user.username }} !{% endblock %}#}

{#{% block content %}
       <h1>Bienvenue sur mon site</h1>
   {% endblock %}#}
<!--DOCUMENT ENTIÈREMENT RÉALISÉ ET COMMENTÉ PAR MAUCLAIR JULIAN-->
<!--Validation W3C executé le ../../....-->
<!DOCTYPE html>
<html>

<!--EN TÊTE-->
<head>
    <!--Langue-->
    <meta charset="utf-8">
    <!--Gestion des MediasQueries-->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--Titre-->
    <title>Zenhurt</title>
    <!--Meta-->
    <meta description="DESCRIPTION">
    <!--Feuille de Style-->
    <link rel="stylesheet" href="assets/css/style.css">
    <!--Police de caractère-->
    <link href="https://fonts.googleapis.com/css?family=" rel="stylesheet">
    <!--Appel de jQuery en ligne & en local-->
    <script type="text/javascript" src="assets/js/jquery.parallax-1.1.3.js"></script>
    <script type="text/javascript" src="assets/js/jquery.scrollTo-1.4.2-min.js"></script>
    <script type="text/javascript" src="assets/js/jquery.localscroll-1.2.7-min.js"></script>
    <script type="text/javascript" src="assets/js/jquery.js"></script>
    <script>
        $(document).ready(function(){
            var ute = 0;
            setInterval(function(){
                if(ute == 0){
                    $('#video').css('display','block');
                    ute++;
                } else{
                    $('#video').css('display','none');
                }
            },2500);
        });
        $(document).ready(function() {
            var ite=0;
            if (ite == 0){
                var page = $('#accueil');
                $('html, body').animate({
                    scrollLeft: $(page).offset().left
                }, 0 );
                return false;
                ite++;
            }
        });
        $(document).ready(function() {
            $('.js-scrollTo').on('click', function() {
                var page = $(this).attr('href');
                var speed = 750;
                $('html, body').animate({
                    scrollLeft: $(page).offset().left
                }, speed );
                return false;
            });
        });
        function ouvre_actions(page){
            if(page == 'dissimulation'){
                page = 'pages_actions/dissimulation.html';
            } else if(page == 'secret'){
                page = 'pages_actions/secret.html';
            } else if(page == 'concurrence'){
                page = 'pages_actions/concurrence.html';
            } else if(page == 'cadeau'){
                page = 'pages_actions/cadeau.html';
            }
            window.open(page,"nom_popup","menubar=no, status=no, scrollbars=no, menubar=no, width=1200, height=300, position:absolute, top=40%, left=40%");
        }
    </script>
</head>
<!--FIN EN TÊTE-->

<!--CORPS-->

<body>
{% block content %}
{% endblock %}
</body>
<!--FIN CORPS-->

</html>
<!--FIN DOCUMENT-->